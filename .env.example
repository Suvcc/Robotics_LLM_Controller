# Required only when config.openai.yaml uses the OpenAI endpoint.
OPENAI_API_KEY=

# Required by aliengo-server. Use a random value of at least 12 characters.
ALIENGO_SERVER_PASSCODE=replace-this-with-a-long-random-secret
